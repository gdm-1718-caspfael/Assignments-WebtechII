

function showBlock(block) {
    for (let block = document.getElementsByClassName("memory-block"); block < array.length; block++) {
        const element = array[block];
        
    }
    block.style.opacity="";
}